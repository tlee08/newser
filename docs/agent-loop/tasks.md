# Agent Loop Backlog

## Ready

- [x] Add unit tests for `buildDailyQuiz`.
- Add a production API deployment option so NewsAPI keys are never exposed outside Vite dev.
- Improve question generation with category-aware prompt templates.
- Add daily seeding so the same five articles produce stable option order for a day.
- Add accessible keyboard focus polish for answer cards.

## Later

- Add streaks and share cards.
- Add category selection.
- Add regional editions.
- Add image-based question templates when NewsAPI articles include usable images.
- Add OpenAI-generated question writing only if the app gains a server-side generation layer.
